if command -v gitleaks >/dev/null 2>&1; then
  gitleaks git --staged --config .gitleaks.toml || exit 1
else
  echo "gitleaks not installed locally — skipping local secret scan (install gitleaks to enable it)."
fi
